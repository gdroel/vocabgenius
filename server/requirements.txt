# The notification listener itself is stdlib-only. PyJWT + cryptography are
# needed to sign the App Store Connect API token for the "Request Test
# Notification" dashboard button.
PyJWT==2.10.1
cryptography==44.0.0
