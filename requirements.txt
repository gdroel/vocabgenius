# pipserver (tools/appstore_notification_listener.py) uses only the Python
# standard library — no third-party dependencies. This file exists so Render's
# Python build step has something to install against.
